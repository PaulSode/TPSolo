const Loader = () => (
  <div style={styles.loader}>⏳ Chargement en cours...</div>
);

const styles = {
  loader: {
    textAlign: "center",
    padding: "50px",
    fontSize: "20px",
    color: "#00ff9f",
  },
};

export default Loader;
